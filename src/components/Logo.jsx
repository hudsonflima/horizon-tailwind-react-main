import React from 'react';

const Logo = () => {
    return (
        <img
            src="/logo.png"  // Caminho absoluto para a imagem
            alt="Logo"
            className="h-8"
        />
    );
};

export default Logo;
